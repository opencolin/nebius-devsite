// Lightweight stylelint stub. The Nebius web team can swap in
// `@gravity-ui/stylelint-config` once they're ready to align with the
// Gravity team's rule set (currently pinned to stylelint 15.x).
module.exports = {
  rules: {},
};
